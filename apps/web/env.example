AUTH0_SECRET='use [openssl rand -hex 32] to generate a 32 bytes value'
AUTH0_BASE_URL='http://localhost:3000'
AUTH0_ISSUER_BASE_URL='https://YOUR_AUTH0_DOMAIN.auth0.com'
AUTH0_CLIENT_ID='YOUR_AUTH0_CLIENT_ID'
AUTH0_CLIENT_SECRET='YOUR_AUTH0_CLIENT_SECRET'
PYTHON_BACKEND_URL='http://127.0.0.1:8000'
# Production: PYTHON_BACKEND_URL='https://veritas-ml-core-api-simr6.ondigitalocean.app'
GEMINI_API_KEY='YOUR_GEMINI_API_KEY'

# Database (DigitalOcean PostgreSQL)
# IMPORTANT FOR VERCEL: Use the connection pool URL if available from DigitalOcean
# Connection pool: postgresql://username:password@host:port/database?sslmode=require&pgbouncer=true
# Direct connection: postgresql://username:password@host:port/database?sslmode=require
DATABASE_URL='postgresql://username:password@host:port/database?sslmode=require'

# SSL Configuration (Optional)
# For strict SSL with certificate verification, download CA cert to certs/ca-certificate.crt
# Run: ./download-ca-cert.sh (or see ENABLE_STRICT_SSL.md)
# 
# Custom certificate path (optional):
# DATABASE_SSL_CA_PATH='/custom/path/to/ca-certificate.crt'
# 
# Disable SSL (not recommended for production):
# DATABASE_SSL='false'
